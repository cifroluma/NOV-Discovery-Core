# Python
venv/
.venv/
__pycache__/
*.pyc

# Continue.dev editor config (local, untouched template)
.continue/

# Result files produced by running the pipeline (regenerate locally)
*.csv
*.pdbqt
ligands/
dock_res/

# ...but keep files that are inputs/checked-in examples, not generated results
!check/receptor.pdbqt
!generate/example_output.csv
